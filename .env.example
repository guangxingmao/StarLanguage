# 复制为 .env 后按需修改，docker-compose 会读取
# cp .env.example .env

# 服务端口（宿主机映射）
PORT=3001

# 腾讯云混元（AI 对话/识图）
TENCENT_REGION=ap-guangzhou
TENCENT_SECRET_ID=your_secret_id
TENCENT_SECRET_KEY=your_secret_key

# PostgreSQL（docker-compose 中 db 服务）
POSTGRES_USER=starknow
POSTGRES_PASSWORD=starknow
POSTGRES_DB=starknow
# 使用 5433 避免与本机 PostgreSQL(5432) 冲突
POSTGRES_PORT=5433
